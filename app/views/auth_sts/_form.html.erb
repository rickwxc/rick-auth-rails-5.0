<%= form_for(auth_st) do |f| %>
  <% if auth_st.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(auth_st.errors.count, "error") %> prohibited this auth_st from being saved:</h2>

      <ul>
      <% auth_st.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :auth_gj_id %>
    <%= f.number_field :auth_gj_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
