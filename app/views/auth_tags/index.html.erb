<p id="notice"><%= notice %></p>

<h1>Category</h1>

<a href="/auth_tag2tags">
  Relation
  </a>
<a href="/auth_website2tags">
  Host2category
  </a>

<a href="/auth_tag_merge">
	Category Merge
</a>

<table class='table'>
  <thead>
    <tr>
      <th>Name</th>
      <th></th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @auth_tags.each do |auth_tag| %>
      <tr>
        <td><%= auth_tag.name %></td>
		  <td>
			  <%= link_to 'Add Img', new_img_path(:uuid => auth_tag.id, :modelname => auth_tag.class.name), target: "_blank"  %>
			  |
	<% d = AuthDiscount.has_discount(auth_tag.class.name, auth_tag.id) %>
	<%if d  %>
		<%= link_to d, edit_auth_discount_path(d), target: "_blank"  %>
	<% else %>
		<%= link_to 'Add Discount', new_auth_discount_path(:obj_id => auth_tag.id, :model => auth_tag.class.name), target: "_blank"  %>
	<% end %>
		  </td>
        <td><%= link_to 'Edit', edit_auth_tag_path(auth_tag) %></td>
        <td><%= link_to 'Destroy', auth_tag, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Auth Tag', new_auth_tag_path %>
<hr />

  <%AuthWebsite.all.each do |web| %> 
	  <b> <%=web.url  %> </b>
	  <!--
	<% d = AuthDiscount.has_discount(web.class.name, web.id) %>
	<%if d  %>
		<%= link_to d, edit_auth_discount_path(d), target: "_blank"  %>
	<% else %>
			  <%= link_to 'Add website Discount', new_auth_discount_path(:obj_id => web.id, :model => web.class.name), target: "_blank"  %>
	<% end %>
	-->

			  <br />
			  <br />
<table class='table table-bordered'>
	  <tr>
  <%web.nav_tags.each do |t| %> 
	  <td>
		  <%=t.name  %> 

		  <%t.get_child.each do |t2| %> 
			  <br />
				  &nbsp; &nbsp; <%=t2.name %> 
			  <%t2.get_child.each do |t3| %> 
				  <br />
				  &nbsp; &nbsp;&nbsp; &nbsp; <%=t3.name %> 
			  <%end %> 
		  <%end %> 
	  </td>
  <%end  %> 
	  </tr>
</table>
  <%end  %> 


