<p id="notice"><%= notice %></p>

<h1>Auth Website2tags</h1>

<table>
  <thead>
    <tr>
      <th>Auth website</th>
      <th>Auth tag</th>
      <th>Idx</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @auth_website2tags.each do |auth_website2tag| %>
      <tr>
        <td><%= auth_website2tag.auth_website_id %></td>
        <td><%= auth_website2tag.auth_tag_id %></td>
        <td><%= auth_website2tag.idx %></td>
        <td><%= link_to 'Show', auth_website2tag %></td>
        <td><%= link_to 'Edit', edit_auth_website2tag_path(auth_website2tag) %></td>
        <td><%= link_to 'Destroy', auth_website2tag, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Auth Website2tag', new_auth_website2tag_path %>
