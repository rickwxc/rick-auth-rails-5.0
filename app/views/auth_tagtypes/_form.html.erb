<%= form_for(auth_tagtype) do |f| %>
  <% if auth_tagtype.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(auth_tagtype.errors.count, "error") %> prohibited this auth_tagtype from being saved:</h2>

      <ul>
      <% auth_tagtype.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
