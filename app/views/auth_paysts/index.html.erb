<p id="notice"><%= notice %></p>

<h1>Pay status</h1>

<table class='table'>
  <thead>
    <tr>
      <th>status</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @auth_paysts.each do |auth_payst| %>
      <tr>
        <td><%= auth_payst.auth_st %></td>
        <td><%= link_to 'Show', auth_payst %></td>
        <td><%= link_to 'Edit', edit_auth_payst_path(auth_payst) %></td>
        <td><%= link_to 'Destroy', auth_payst, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Auth Payst', new_auth_payst_path %>
