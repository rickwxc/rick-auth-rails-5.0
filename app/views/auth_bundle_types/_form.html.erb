<%= form_for(auth_bundle_type) do |f| %>
  <% if auth_bundle_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(auth_bundle_type.errors.count, "error") %> prohibited this auth_bundle_type from being saved:</h2>

      <ul>
      <% auth_bundle_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
