<%= form_for(auth_obj_attr_group) do |f| %>
  <% if auth_obj_attr_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(auth_obj_attr_group.errors.count, "error") %> prohibited this auth_obj_attr_group from being saved:</h2>

      <ul>
      <% auth_obj_attr_group.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
