<%= form_for(um) do |f| %>
  <% if um.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(um.errors.count, "error") %> prohibited this um from being saved:</h2>

      <ul>
      <% um.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %>
    <%= f.text_field :user_id %>
  </div>

  <div class="field">
    <%= f.label :content %>
    <%= f.text_field :content %>
  </div>

  <div class="field">
    <%= f.label :provider %>
    <%= f.text_field :provider %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
